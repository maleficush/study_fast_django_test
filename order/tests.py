from django.test import TestCase

# Create your tests here.
class ShopModelTest(TestCase):
    def test_sample(self):
        temp = True
        self.assertIs(temp, False)
    def test_sample(self):
        temp = True
        self.assertIs(temp, False)
    def test_sample(self):
        temp = True
        self.assertIs(temp, False)